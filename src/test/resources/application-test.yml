spring:
  datasource:
    url: jdbc:h2:mem:testdb
    driverClassName: org.h2.Driver
    username: sa
    password: password

  jpa:
    hibernate:
      ddl-auto: create-drop
    show-sql: true

funds_app:
  jwt:
    secret: test-secret-key-123456-test-secret-key-123456
    expiration: 3600000